# OxTS lever arm message

# Header
std_msgs/Header header      # Standard ROS message header

# Lever arm name (e.g. GAP)
string lever_arm_id

# Offset (x, y, z)
float64 offset_x
float64 offset_y
float64 offset_z